Run the /specify command to specify the details of a requirement.
Then
Run the /planify command in another subagent session  to generate implementation plan files from the spec.
Then
Run the /codify command in another subagent session to implement the requirement with working code and unit tests.